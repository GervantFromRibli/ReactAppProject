const headerStyle = {
    backgroundColor: 'gold', 
    fontFamily: 'Georgia, serif'
}

const parentDiv = {
    margin: '1rem', 
    textAlign: 'center'
}

const childDiv = {
    display: 'inline-block', 
    padding: '1rem'
}

const firstChild = {
    float: 'left'
}

const linkStyle = {
    textDecoration: 'none'
}

const footerStyle = {
    position: 'absolute', 
    bottom: 0, 
    width: '100%', 
    height: '2.5rem',
    backgroundColor: 'gold'
}

const divStyle = {
    display: 'flex', 
    justifyContent: 'center', 
    alignItems: 'center'
}

const loginForm = {
    display:'flex', 
    marginLeft: '44vw', 
    marginTop: '15vw'
}

const loginButton = {
    display: 'flex', 
    justifyContent: 'center'
}

const warnForm = {
    top: '37%', 
    left: '37%', 
    position: 'absolute'
}

const elementBaseStyle = {
    marginLeft: 15, 
    marginTop: 15
}

const tableBordRadius = {
    borderRadius: 10
}

const inputRadius = {
    borderRadius: 6, 
    margin: 4
}

const mainDiv = {
    marginLeft: 15
}

export {headerStyle, parentDiv, childDiv, firstChild, linkStyle, 
    footerStyle, divStyle, loginForm, loginButton, warnForm, elementBaseStyle,
    tableBordRadius, inputRadius, mainDiv}